specVersion: "v1"
serviceId: "portafolioweb"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "portafolioweb-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
